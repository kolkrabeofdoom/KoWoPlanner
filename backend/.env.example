# Kopieren nach .env und Werte setzen (NIEMALS die echte .env committen!)
PORT=3001
DATABASE_URL="postgresql://postgres:DEIN_DB_PASSWORT@localhost:5432/kowoplanner?schema=public"
# Erzeugen z.B. mit: openssl rand -base64 48
JWT_SECRET=
